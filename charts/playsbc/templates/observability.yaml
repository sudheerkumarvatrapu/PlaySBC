{{- $observability := .Values.observability | default dict }}
{{- $obsEnabled := get $observability "enabled" | default false }}
{{- $prometheus := get $observability "prometheus" | default dict }}
{{- $serviceMonitor := get $prometheus "serviceMonitor" | default dict }}
{{- $rules := get $prometheus "rules" | default dict }}
{{- $grafana := get $observability "grafana" | default dict }}
{{- $dashboard := get $grafana "dashboard" | default dict }}
{{- if get $serviceMonitor "enabled" | default false }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "playsbc.fullname" . }}
  labels:
{{ include "playsbc.labels" . | indent 4 }}
{{- with (get $serviceMonitor "labels") }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "playsbc.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  endpoints:
    - port: health
      path: /metrics
      interval: {{ get $serviceMonitor "interval" | default "15s" }}
      scrapeTimeout: {{ get $serviceMonitor "scrapeTimeout" | default "5s" }}
---
{{- end }}
{{- if get $rules "enabled" | default false }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "playsbc.fullname" . }}-alerts
  labels:
{{ include "playsbc.labels" . | indent 4 }}
{{- with (get $rules "labels") }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  groups:
    - name: playsbc.rules
      rules:
        - alert: PlaySBCAdmissionRejectionsHigh
          expr: sum(rate(playsbc_admission_rejections_total[5m])) > 0
          for: 2m
          labels:
            severity: warning
          annotations:
            summary: PlaySBC is rejecting admitted calls.
            description: Call admission rejection rate has been non-zero for more than two minutes.
        - alert: PlaySBCRtpengineControlFailures
          expr: sum(rate(playsbc_rtpengine_control_failures_total[5m])) > 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: RTPengine control path is failing.
            description: PlaySBC is seeing RTPengine offer, answer, query, or delete failures.
        - alert: PlaySBCAiRasaFailures
          expr: sum(rate(playsbc_ai_rasa_failures_total[5m])) > 0
          for: 2m
          labels:
            severity: warning
          annotations:
            summary: AI/Rasa voice turns are failing.
            description: Rasa REST turns are returning errors or falling back.
        - alert: PlaySBCTrunkDown
          expr: min(playsbc_trunk_healthy) == 0
          for: 2m
          labels:
            severity: warning
          annotations:
            summary: One or more PlaySBC trunks are unhealthy.
            description: OPTIONS probing or call failures marked at least one trunk unhealthy.
        - alert: PlaySBCHaNodeDraining
          expr: max(playsbc_ha_node_draining) > 0
          for: 5m
          labels:
            severity: info
          annotations:
            summary: A PlaySBC HA node is draining.
            description: At least one HA node is rejecting new calls because drain mode is enabled.
---
{{- end }}
{{- if or $obsEnabled (get $grafana "enabled" | default false) (get $dashboard "enabled" | default false) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "playsbc.fullname" . }}-grafana-dashboard
  labels:
{{ include "playsbc.labels" . | indent 4 }}
{{- with (get $dashboard "labels") }}
{{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
data:
  playsbc-dashboard.json: |-
    {
      "uid": "playsbc-sbc-lab",
      "title": "PlaySBC Core/Peer SBC Lab",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "10s",
      "tags": ["playsbc", "sbc", "core-peer", "rtpengine", "rasa"],
      "time": {"from": "now-1h", "to": "now"},
      "templating": {
        "list": [
          {
            "name": "job",
            "type": "query",
            "datasource": {"type": "prometheus", "uid": "prometheus"},
            "query": "label_values(playsbc_active_calls, job)",
            "refresh": 1,
            "includeAll": true,
            "allValue": ".*",
            "multi": true,
            "current": {"text": "All", "value": ".*"}
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "type": "stat",
          "title": "Current Active Calls",
          "description": "Instant active-call gauge. This should return to 0 after a regression profile finishes.",
          "gridPos": {"x": 0, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "green"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "(sum(playsbc_active_calls{job=~\"${job:regex}\"}) or vector(0)) + (sum(playsbc_ai_voice_calls_active{job=~\"${job:regex}\"}) or vector(0))", "legendFormat": "active now"}]
        },
        {
          "id": 2,
          "type": "stat",
          "title": "Calls Started In Range",
          "description": "Started calls observed in the selected range. Uses counter increase so Kubernetes profile pod rollovers do not hide traffic.",
          "gridPos": {"x": 4, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "blue"}, "thresholds": {"mode": "absolute", "steps": [{"color": "blue", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "(sum(increase(playsbc_b2bua_calls_total{job=~\"${job:regex}\"}[$__range])) or vector(0)) + (sum(increase(playsbc_ai_voice_calls_total{job=~\"${job:regex}\"}[$__range])) or vector(0))", "legendFormat": "started"}]
        },
        {
          "id": 14,
          "type": "stat",
          "title": "Completed Calls In Range",
          "description": "Completed calls observed in the selected range. For a finished 300-call load profile this should match SIPp's successful-call count.",
          "gridPos": {"x": 8, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "green"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum(increase(playsbc_b2bua_calls_completed_total{job=~\"${job:regex}\"}[$__range])) or vector(0)", "legendFormat": "completed"}]
        },
        {
          "id": 3,
          "type": "stat",
          "title": "SIP Requests Observed",
          "description": "SIP requests observed in the selected range. Uses counter increase across pod rollovers.",
          "gridPos": {"x": 12, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "blue"}, "thresholds": {"mode": "absolute", "steps": [{"color": "blue", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum(increase(playsbc_sip_requests_total{job=~\"${job:regex}\"}[$__range])) or vector(0)", "legendFormat": "requests"}]
        },
        {
          "id": 9,
          "type": "stat",
          "title": "SIP Responses Observed",
          "description": "SIP responses observed in the selected range, including provisional, success, redirection, and error responses.",
          "gridPos": {"x": 16, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "purple"}, "thresholds": {"mode": "absolute", "steps": [{"color": "purple", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum(increase(playsbc_sip_responses_total{job=~\"${job:regex}\"}[$__range])) or vector(0)", "legendFormat": "responses"}]
        },
        {
          "id": 4,
          "type": "stat",
          "title": "Peak Active In Range",
          "description": "Historical high-water mark for active calls in the selected time range. This intentionally stays non-zero while the selected range includes a load run.",
          "gridPos": {"x": 20, "y": 0, "w": 4, "h": 4},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "orange"}, "thresholds": {"mode": "absolute", "steps": [{"color": "orange", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "max_over_time(sum(playsbc_active_calls{job=~\"${job:regex}\"})[$__range:]) or vector(0)", "legendFormat": "peak active"}]
        },
        {
          "id": 5,
          "type": "timeseries",
          "title": "Core/Peer Trunk Health Observed",
          "description": "Shows trunk health samples seen in the selected range. This panel populates when trunk-group profiles run, such as failover, trunk metrics, or OPTIONS health recovery.",
          "gridPos": {"x": 0, "y": 4, "w": 12, "h": 7},
          "fieldConfig": {"defaults": {"color": {"mode": "thresholds"}, "thresholds": {"mode": "absolute", "steps": [{"color": "red", "value": null}, {"color": "green", "value": 1}]}}, "overrides": []},
          "targets": [{"expr": "sum by (realm,trunk) (max_over_time(playsbc_trunk_healthy{job=~\"${job:regex}\"}[$__range]))", "legendFormat": "{{ "{{" }}realm{{ "}}" }}/{{ "{{" }}trunk{{ "}}" }}"}]
        },
        {
          "id": 6,
          "type": "timeseries",
          "title": "SIP Requests Observed By Realm",
          "description": "SIP request increases grouped by realm, direction, and method for the selected range.",
          "gridPos": {"x": 12, "y": 4, "w": 12, "h": 7},
          "fieldConfig": {"defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "blue", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum by (realm,method,direction) (increase(playsbc_sip_requests_total{job=~\"${job:regex}\"}[$__rate_interval]))", "legendFormat": "{{ "{{" }}realm{{ "}}" }}/{{ "{{" }}direction{{ "}}" }} {{ "{{" }}method{{ "}}" }}"}]
        },
        {
          "id": 7,
          "type": "timeseries",
          "title": "SIP Responses Observed By Status",
          "description": "SIP response increases grouped by realm, direction, status code, and status class.",
          "gridPos": {"x": 0, "y": 11, "w": 12, "h": 7},
          "fieldConfig": {
            "defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}},
            "overrides": [
              {"matcher": {"id": "byRegexp", "options": ".* [45]xx$"}, "properties": [{"id": "color", "value": {"mode": "fixed", "fixedColor": "red"}}]}
            ]
          },
          "targets": [{"expr": "sum by (realm,status,status_class,direction) (increase(playsbc_sip_responses_total{job=~\"${job:regex}\"}[$__rate_interval]))", "legendFormat": "{{ "{{" }}realm{{ "}}" }}/{{ "{{" }}direction{{ "}}" }} {{ "{{" }}status{{ "}}" }} {{ "{{" }}status_class{{ "}}" }}"}]
        },
        {
          "id": 10,
          "type": "timeseries",
          "title": "AI/Rasa Turns By Realm",
          "gridPos": {"x": 12, "y": 11, "w": 12, "h": 7},
          "fieldConfig": {
            "defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}},
            "overrides": [
              {"matcher": {"id": "byRegexp", "options": ".*failures/s$"}, "properties": [{"id": "color", "value": {"mode": "fixed", "fixedColor": "red"}}]}
            ]
          },
          "targets": [
            {"expr": "sum by (realm,bot,stt,tts) (rate(playsbc_ai_voice_turns_total{job=~\"${job:regex}\"}[5m]))", "legendFormat": "{{ "{{" }}realm{{ "}}" }}/{{ "{{" }}bot{{ "}}" }} {{ "{{" }}stt{{ "}}" }}/{{ "{{" }}tts{{ "}}" }} turns/s"},
            {"expr": "sum by (realm,bot) (rate(playsbc_ai_rasa_failures_total{job=~\"${job:regex}\"}[5m]))", "legendFormat": "{{ "{{" }}realm{{ "}}" }}/{{ "{{" }}bot{{ "}}" }} rasa failures/s"}
          ]
        },
        {
          "id": 8,
          "type": "stat",
          "title": "Media Negotiated",
          "description": "Answered calls with a negotiated audio codec pair. Labels expose backend, core/peer realms, inbound codec, outbound codec, and transcoding=true/false.",
          "gridPos": {"x": 0, "y": 18, "w": 6, "h": 5},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "teal"}, "thresholds": {"mode": "absolute", "steps": [{"color": "teal", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum(increase(playsbc_media_negotiations_total{job=~\"${job:regex}\"}[$__range])) or vector(0)", "legendFormat": "negotiated"}]
        },
        {
          "id": 11,
          "type": "stat",
          "title": "Transcoding Sessions In Range",
          "description": "Answered calls where the negotiated inbound and outbound audio codecs differ. This uses media-negotiation samples with transcoding=true so HA pod rollovers still show PCMU-to-PCMA evidence.",
          "gridPos": {"x": 6, "y": 18, "w": 6, "h": 5},
          "fieldConfig": {"defaults": {"color": {"mode": "fixed", "fixedColor": "purple"}, "thresholds": {"mode": "absolute", "steps": [{"color": "purple", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum(increase(playsbc_media_negotiations_total{job=~\"${job:regex}\",transcoding=\"true\"}[$__range])) or vector(0)", "legendFormat": "transcoding"}]
        },
        {
          "id": 12,
          "type": "timeseries",
          "title": "Codec Negotiation Matrix",
          "description": "Negotiated audio codec pairs by backend. Example: rtpengine PCMU -> PCMA transcoding=true.",
          "gridPos": {"x": 12, "y": 18, "w": 12, "h": 5},
          "fieldConfig": {"defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum by (backend,inbound_codec,outbound_codec,transcoding) (increase(playsbc_media_negotiations_total{job=~\"${job:regex}\"}[$__rate_interval]))", "legendFormat": "{{ "{{" }}backend{{ "}}" }} {{ "{{" }}inbound_codec{{ "}}" }} -> {{ "{{" }}outbound_codec{{ "}}" }} transcode={{ "{{" }}transcoding{{ "}}" }}"}]
        },
        {
          "id": 18,
          "type": "timeseries",
          "title": "Transcoding By PlaySBC Node",
          "description": "Codec-mismatch sessions grouped by active-active PlaySBC node.",
          "gridPos": {"x": 0, "y": 43, "w": 24, "h": 7},
          "fieldConfig": {"defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "purple", "value": null}]}}, "overrides": []},
          "targets": [{"expr": "sum by (node,backend,inbound_codec,outbound_codec) (increase(playsbc_media_negotiations_total{job=~\"${job:regex}\",transcoding=\"true\"}[$__rate_interval]))", "legendFormat": "{{ "{{" }}node{{ "}}" }} {{ "{{" }}backend{{ "}}" }} {{ "{{" }}inbound_codec{{ "}}" }} -> {{ "{{" }}outbound_codec{{ "}}" }}"}]
        },
        {
          "id": 13,
          "type": "timeseries",
          "title": "RTPengine Core -> Peer",
          "gridPos": {"x": 0, "y": 23, "w": 24, "h": 7},
          "fieldConfig": {
            "defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}},
            "overrides": [
              {"matcher": {"id": "byRegexp", "options": ".*failures$"}, "properties": [{"id": "color", "value": {"mode": "fixed", "fixedColor": "red"}}]}
            ]
          },
          "targets": [
            {"expr": "sum by (from_realm,to_realm) (playsbc_rtpengine_media_sessions_active{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}from_realm{{ "}}" }} -> {{ "{{" }}to_realm{{ "}}" }} active"},
            {"expr": "sum by (from_realm,to_realm) (increase(playsbc_rtpengine_control_failures_total{job=~\"${job:regex}\"}[$__range]))", "legendFormat": "{{ "{{" }}from_realm{{ "}}" }} -> {{ "{{" }}to_realm{{ "}}" }} failures"}
          ]
        },
        {
          "id": 15,
          "type": "timeseries",
          "title": "PlaySBC Active Calls By Node",
          "description": "Active calls per PlaySBC replica. In active-active mode the node label maps to the StatefulSet pod identity.",
          "gridPos": {"x": 0, "y": 30, "w": 12, "h": 7},
          "fieldConfig": {"defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
          "targets": [
            {"expr": "sum by (node,from_realm,to_realm) (playsbc_active_calls{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} {{ "{{" }}from_realm{{ "}}" }} -> {{ "{{" }}to_realm{{ "}}" }}"}
          ]
        },
        {
          "id": 16,
          "type": "timeseries",
          "title": "HA Shared Registrar/Dialog State",
          "description": "Shared state evidence used by the active-active lab: registrar rows, dialog rows, answered dialog rows, and restore attempts.",
          "gridPos": {"x": 12, "y": 30, "w": 12, "h": 7},
          "fieldConfig": {"defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
          "targets": [
            {"expr": "sum by (cluster,node) (playsbc_ha_shared_registrations{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} shared registrations"},
            {"expr": "sum by (cluster,node) (playsbc_ha_shared_dialogs{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} shared dialogs"},
            {"expr": "sum by (cluster,node) (playsbc_ha_shared_answered_dialogs{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} answered dialogs"},
            {"expr": "sum by (cluster,node) (increase(playsbc_ha_dialog_restores_total{job=~\"${job:regex}\"}[$__range]))", "legendFormat": "{{ "{{" }}node{{ "}}" }} restores"}
          ]
        },
        {
          "id": 17,
          "type": "timeseries",
          "title": "HA Node State",
          "description": "Configured node count and drain state. Red is reserved for draining nodes.",
          "gridPos": {"x": 0, "y": 37, "w": 24, "h": 6},
          "fieldConfig": {
            "defaults": {"color": {"mode": "palette-classic"}, "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}},
            "overrides": [
              {"matcher": {"id": "byRegexp", "options": ".*draining.*"}, "properties": [{"id": "color", "value": {"mode": "fixed", "fixedColor": "red"}}]}
            ]
          },
          "targets": [
            {"expr": "sum by (cluster,node) (playsbc_ha_configured_nodes{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} configured nodes"},
            {"expr": "sum by (cluster,node) (playsbc_ha_node_draining{job=~\"${job:regex}\"})", "legendFormat": "{{ "{{" }}node{{ "}}" }} draining"}
          ]
        }
      ]
    }
{{- end }}
